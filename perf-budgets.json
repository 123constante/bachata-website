{
  "//": "First-load JS budgets (gzip KB) enforced by scripts/check-bundle-budget.mjs (run via `npm run check:bundle-budget` after `npm run build`). Raise a budget only deliberately, in the same PR that grows the graph, with justification in the PR description. The bundle-budget job BLOCKS (continue-on-error was dropped in #187); it is not warn-only.",
  "//home": "255 -> 215 on 2026-08-07 by P6, the closing phase of the supabase-defer arc. Measured 202.9 KB gz across 64 files with vendor-supabase NOT in first load and 0 direct pullers -- the arc's completion proof, and -41.3 KB against the pre-arc baseline. 12.1 KB of headroom, about 2.4 dependency batches at #187's observed +5.0 KB per 53-package batch, which is deliberately the SAME posture the old 255/244.2 pair carried (10.8 KB); the budget got tighter because the route got smaller, not because the tolerance changed. HISTORY, kept because it is the whole reason the arc existed: 245 -> 255 on 2026-08-05 at 244.2 measured, when vendor-supabase (43.3 KB gz) sat in home's first load -- auth-js 46%, realtime-js 17%, storage-js 17%, storage entirely unused (zero `.storage.from(` in src/). None of it was separable: SupabaseClient's constructor eagerly builds every sub-client, so no bundler can drop realtime or storage while `createClient` is imported statically. Deferring the client behind getSupabase() was the only lever, and it took six phases. BEFORE RAISING THIS AGAIN, read `pullerRatchet` below: weight and graph membership are different questions, and P5 measured a regression that read as a 43.8 KB WIN against its budget. UNCHANGED at 215 by P1 of the vendor-cost arc on 2026-08-14, and that is worth a line because P1 went looking for a raise and did not need one. P1 cut home from 64 first-load chunks to 42 -- the change this whole arc exists to make, since Vercel meters edge REQUESTS and the account is at 180% of allowance on them -- and it did it for -0.9 KB: 202.9 -> 202.0 KB gz. Four chunking rules were built and measured against this row before one earned its place; the two rejected wide ones are written up in vite.config.ts so they are not re-run. The third, a single hand-curated Radix group, is the one that matters here: it made home heavier and a raise to 225 was drafted to absorb it. What withdrew the raise was MEASURING the homepage's first-load package set instead of reasoning about it -- a large fraction of that group was not in the graph at all, and those packages became a second chunk that only the routes mounting a dialog pay for. Read this before raising the budget for a chunking change: the raise was a symptom of an unmeasured list, not the price of the win. Live figures come from `npm run check:bundle-budget` and the baseline block below; deltas are not restated in this prose, because a number copied into a comment has no writer keeping it true.",
  "//event": "385 -> 375 on 2026-08-14 by P1 of the vendor-cost arc. This is a TIGHTENING, and it is here because the file was applying its own anti-slack rule to one number and not the other: the same commit lowered event's pullerRatchet 3 -> 2 because leaving a won allowance unclaimed 'decays into a ceiling with slack, and the next regression hides inside the slack', while event's KB budget kept 385 against a measured 358.3 -- 27.3 KB of exactly that slack, on the heaviest route in the app. The KB budget has no under-check to force this (the ratchet does), which is why it needed saying rather than firing. 375 leaves 16.7 KB, roughly the 19.2 KB the 365.8/385 pair carried and about three of #187's 5.0 KB dependency batches. Event carried P1's largest win in both columns, dropping from 93 first-load chunks to 52 and getting lighter as well. Nearly all of the KB is the consolidation itself -- fewer, larger chunks gzip better and drop the per-file overhead -- NOT the tslib move, which is worth a fraction of a KB here and whose value is the puller it removed rather than the bytes. Event still carries vendor-supabase in its first load; nothing left the graph.",
  "//auth": "Added by P5 of the supabase-defer arc. This route is budgeted PRIMARILY so requiredFirstLoad below has somewhere to hang -- the auth pages (/auth, /auth/callback) are not framework routes, they live on the catchall, so the catchall's graph IS the auth path's graph. Measured 277.4 KB gz across 58 files at ebc67e8. Budget set to 310 deliberately generously (32.6 KB headroom, ~12%): #187's 53-package batch cost +5.0 KB, so that is roughly six routine dependency batches of room. The point is that this route must never red-light for drift unrelated to the edge it exists to guard -- a budget red here would be read as an auth-path failure and waste the reader's time. It is NOT a target to optimise against; the catchall deliberately carries vendor-supabase (see requiredFirstLoad). FIGURES ABOVE ARE ebc67e8's AND ARE NOW STALE IN BOTH COLUMNS -- P1 re-measured this route on 2026-08-14 and both the KB and the file count moved. They are left in place rather than overwritten because they are what the 310 was CHOSEN against, and that reasoning is the point of the note; the live numbers are in attribution.baseline below, which is the row anything automated reads, and the headroom follows from those. P1 left this route lighter than it found it despite adding the vendor-icons chunk to its graph, on consolidation alone -- it does NOT pull vendor-ui-modal, which is in /event's first load only.",
  "routes": {
    "home (/city/:slug)": {
      "entries": [
        "app/entry.client.tsx",
        "app/root.tsx",
        "app/routes/home.tsx"
      ],
      "maxFirstLoadGzipKB": 215
    },
    "event (/event/:id)": {
      "entries": [
        "app/entry.client.tsx",
        "app/root.tsx",
        "app/routes/event.tsx"
      ],
      "maxFirstLoadGzipKB": 375
    },
    "auth (/auth/callback, via catchall)": {
      "entries": [
        "app/entry.client.tsx",
        "app/root.tsx",
        "app/routes/catchall.tsx"
      ],
      "maxFirstLoadGzipKB": 310
    }
  },
  "//requiredFirstLoad": "The INVERSE of the budget, and of the ratchet P6 adds. Everywhere else this file asks 'has the tracked module stayed OUT of this route's first-load graph'. Here it asks the opposite: client.ts sets detectSessionInUrl:true, so auth-js parses the magic-link fragment at CONSTRUCTION, and the auth path needs that construction to happen during initial module evaluation -- before hydration, before the router can rewrite the URL. Deferring it there loses a real user's session silently and tells them their link expired. Until P5 that edge existed only by ACCIDENT (AuthGuard.tsx and ListingRequestForm.tsx were merely unconverted), so the arc's own remaining work would have removed it with nothing failing. src/integrations/supabase/eagerAuthClient.ts makes it intentional; this makes it enforced. BLOCKING, unlike the report-only attribution above: a missing required edge exits 1.",
  "requiredFirstLoad": [
    {
      "route": "auth (/auth/callback, via catchall)",
      "module": "src/integrations/supabase/client.ts",
      "why": "detectSessionInUrl parses the magic-link fragment at client construction; the auth path must construct eagerly, not behind getSupabase(). Held by src/integrations/supabase/eagerAuthClient.ts."
    }
  ],
  "//pullerRatchet": "P6, the arc's closing guard. P0 built the puller count as an INSTRUMENT -- it printed, nothing enforced -- which was right while the number was still moving: home was being driven 3 -> 0 and a guard pinned to a number you are about to change is noise. The number has stopped moving, so it becomes a contract. WHY A RATCHET AND NOT A BUDGET: the KB budget cannot catch this class, and P5 proved it -- simulating the regression left the auth route at 233.6 KB against a 310 budget, so the defect read as a 43.8 KB WIN. Weight and graph membership are different questions and only one of them is what this arc bought. BOTH DIRECTIONS BLOCK. Over the allowance is a regression: vendor-supabase has re-entered a graph six phases removed it from, and the puller + module-edge lists printed by the guard name the exact import to route through getSupabase(). UNDER the allowance also fails, because an allowance nobody tightens stops describing the code -- it decays into a ceiling with slack and the next regression hides in the slack; the fix is a one-line edit in the PR that earned the win, which is when the number is best understood. Every budgeted route must have an entry: a budgeted route with no ratchet can take on unlimited pullers silently. home 0 is the arc's completion proof and the row that must never rise. event 2 and auth 2 are pins, not targets -- auth's edge is REQUIRED (see requiredFirstLoad above), so its allowance can never honestly reach 0. event was 3 until 2026-08-14, when P1 of the vendor-cost arc moved tslib out of vendor-supabase (it rides with vendor-react -- see the tslib branch in vite.config.ts, which explains why it is not a chunk of its own) and the third puller vanished. That puller was never real in the sense the ratchet means: tslib is a shared helper runtime that happens to be installed as a Supabase dependency, so rollup filed it inside vendor-supabase and any chunk compiled with tslib helpers acquired a static edge to the 43 KB Supabase client to reach three one-line functions. The ratchet counted that edge honestly -- the chunk really did import vendor-supabase -- which is exactly why the row is a MEASUREMENT and not a judgement, and why it went UNDER and refused to pass until the win was written down.",
  "pullerRatchet": {
    "home (/city/:slug)": 0,
    "event (/event/:id)": 2,
    "auth (/auth/callback, via catchall)": 2
  },
  "attribution": {
    "//": "Report-only, added by P0 of the supabase-defer arc (~/.claude/plans/arc-defer-supabase-client.md). Names one chunk whose FIRST-LOAD PULLERS each budgeted route reports: the chunks holding a direct static edge to it, a shortest path from a route entry, and the first-party modules each puller contains (from the sourcemap). The puller count is the arc's work-list -- it shrinks by one per phase, and 0 pullers on home is the completion proof. Nothing here is enforced; P6 turns the count into a ratchet.",
    "//trackedChunkName": "Matched against the manifest node's chunk `name`, i.e. the manualChunks group key in vite.config.ts -- NOT the hashed filename, which changes on every content edit. If that group is ever renamed the guard hard-fails rather than reporting a reassuring 0: a tracker that silently measures nothing is the failure mode rule R1 of check-script-conventions.mjs exists to catch.",
    "trackedChunkName": "vendor-supabase",
    "//trackedModule": "The first-party module that leads INTO trackedChunkName, and the target of the module-level edge report. A chunk name cannot serve: no import statement names a chunk, so the chunk-level puller list can say where the weight is but never which import to delete. That gap is why 'is this the last edge?' was answered by `grep -rl integrations/supabase/client` three times and was wrong three times -- grep sees direct importers of a string, not a barrel re-export (@/lib/seo -> useEntitySlugOrId), a provider three components deep (root -> SearchProvider -> SearchOverlay -> searchClickTelemetry), or a loader helper (home -> detailLoader -> resolvePublicEventRef). All three misses were of exactly those shapes, and two of them were recorded in the arc plan as out of scope on the strength of a grep. If this module stops being the way in (client.ts retired, or the client constructed somewhere else), repoint it -- the guard hard-fails if the path is not a file, rather than reporting 0 edges for every route.",
    "trackedModule": "src/integrations/supabase/client.ts",
    "baseline": {
      "//": "RE-BASELINED by P6 at the arc's close, 2026-08-07. Measured at afd7996 (P5's merge onto main) via npm run build + npm run check:bundle-budget, with only scripts/ and .claude/ edits applied on top -- neither is in the client bundle, so these ARE afd7996's figures. Deltas printed by the guard are against this row; it is rewritten only by a phase that deliberately re-baselines, and never to paper over a regression. WHY RE-BASELINE HERE, since that rule is otherwise strict: the previous row was 138390a, PRE-arc, and home has since dropped 41.3 KB. Leaving it would print '-41.3 KB' on every future run, where it reads as 'this PR saved 41 KB' -- a number meaning something other than what it says, which is the failure mode this file exists to prevent. All three rows now come from ONE commit, which also retires the split-measurement caveat the auth row carried (it was measured at ebc67e8, not 138390a, because it was not budgeted then). PRE-ARC FIGURES, kept so the arc's win stays legible: home 244.2 KB / 61 chunks / 3 pullers (138390a); event 363.5 / 89 / 4 (138390a); auth 277.4 / 58 / 2 (ebc67e8). RE-BASELINED AGAIN 2026-08-14 by P1 of the vendor-cost arc, for the same reason the last one gave: the CHUNK column moved by a third to nearly a half on every route (home 64 -> 42, event 93 -> 52, auth 59 -> 36) and leaving the old row would have printed '-41 chunks' on every future run, where it reads as 'this PR removed 41 chunks'. WHY THE COMMIT FIELD IS NOT A BARE SHA HERE: these figures are 55847dd's client bundle built with THIS commit's vite.config.ts manualChunks, and the merge sha does not exist while the file is being written. That config change is the ONLY input to them -- no source file in the client graph is touched by this PR -- so the row is reproducible from this tree with `npm run build && npm run check:bundle-budget`, which is the property the sha was ever standing in for. PREVIOUS ROW, kept for the same legibility reason: home 202.9 / 64 / 0, event 365.8 / 93 / 3, auth 278.7 / 59 / 2, vendor-supabase 43.3 KB (afd7996). The tracked chunk lost 0.3 KB because tslib moved out of it, not because Supabase shrank.",
      "commit": "55847dd+p1-chunk-consolidation",
      "trackedChunkGzipKB": 43.0,
      "routes": {
        "home (/city/:slug)": {
          "firstLoadGzipKB": 202.0,
          "chunks": 42,
          "pullers": 0
        },
        "event (/event/:id)": {
          "firstLoadGzipKB": 358.3,
          "chunks": 52,
          "pullers": 2
        },
        "auth (/auth/callback, via catchall)": {
          "firstLoadGzipKB": 276.3,
          "chunks": 36,
          "pullers": 2
        }
      }
    }
  }
}
