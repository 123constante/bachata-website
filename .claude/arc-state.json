{
  "version": 1,
  "arc": "operating-model-v2",
  "phase": 9.1,
  "set_at": "2026-08-04T09:32:00Z",
  "scope": [
    ".claude/arc-state.json",
    ".github/workflows/architecture-guard.yml",
    "package.json",
    "scripts/audit-images.mjs",
    "scripts/check-image-widths.mjs",
    "scripts/lib/stripSource.mjs",
    "scripts/pre-ship.mjs",
    "tests/imageWidths.test.ts"
  ],
  "note": "PHASE NUMBERING FIRST, because it is the reason this entry is 9.1 and not 8. This branch was cut while the arc was at phase 8 and declared phase 8 here; #186 then landed phase 9 -- the ARC-CLOSE phase -- on main, and this branch merged main afterwards, resolving this file to its own phase 8. Merging that would have REWOUND main from 9 to 8: the arc-close record gone, and the arc-checkpoint hook pinning the next session to a superseded phase and a scope that does not describe this ship (review finding). The phase number must be monotonic in main's history, so this interposed ship is 9.1 and carries phase 9's durable content forward. DELIVERABLE: the image-width guard that makes the PR #178 defect class unrepeatable, plus eleven /code-review findings folded on 2026-08-04 -- four fail-opens in the guard itself (an unparseable call site dropped silently; a ZERO-calls tripwire that could never fire because findCalls claimed imageCdn.ts's own `export function` lines; rules 1 and 2 regexing imageCdn.ts unstripped so a doc example could answer them; a stray quote in an argument list discarding the whole call), a CRLF line-continuation bug in the shared stripper on the only line ending this repo uses, two classifier false-reds (`320 as number`, `2 * srcWidthFor(48)`), the named-constant evasion `const THUMB = 80` now covered as rule 5, a false one-directional safety claim replaced with the measured result, an off-by-one chain count in pre-ship, the smoke gate realigned to CI's parallelism plus retries, and a stray 0x08 byte that was committed inside a comment in check-image-widths.mjs. CARRIED FORWARD FROM PHASE 9, deferred by Ricky's decision 2026-08-04 and not to be relitigated: the coverless-event OG card stays deferred with its ledger row (/api/og/card letterboxes the cover with no text overlay, so a coverless event gets the venue photo rather than a branded card) and #185 guards the data instead via check #62; and the check-search-public-v5 upcoming-only window is STILL OPEN -- #136 (11fcae3) shipped a MITIGATION, not the specified fix, widening the bound to a measured 6h grace + 5d max span heuristic where the slot asked for a rollover-boundary anchor plus a fixed-clock both-direction proof. Do NOT record #136 as carrying it. Authoring pin: claude-opus-5 / high; this session runs claude-opus-5[1m] / low -- effort below the pin, declared and recorded, not a halt. Worktree C:/tmp/wc-184-wt on branch ci/image-width-contract-fixes, cut because a second session held ci/image-width-contract live."
}
