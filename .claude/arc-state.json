{
  "version": 1,
  "arc": "supabase-defer",
  "phase": 0,
  "set_at": "2026-08-05T07:05:42Z",
  "scope": [
    ".claude/arc-state.json",
    "scripts/check-bundle-budget.mjs",
    "perf-budgets.json",
    ".github/workflows/perf-budget.yml",
    "package.json",
    "scripts/script-conventions-allowlist.json"
  ],
  "note": "PHASE 0 -- instrument before change (PERF class rule: harness PR precedes change PRs). Arc goal: get vendor-supabase (43.3 KB gz) out of home's first-load static import graph by making createClient reachable only via await import(). Plan: ~/.claude/plans/arc-defer-supabase-client.md. P0 ships NO app-code change. It (a) re-measures home's first-load gz at 138390a as the arc's baseline -- the 43.3/244.2 figures are INHERITED from session-handover-2026-08-05 and have NOT been re-measured, so if P0 disagrees with them, P0 wins; and (b) extends the bundle-budget tooling to report WHICH first-load chunks pull vendor-supabase, so each later phase gets a measured, shrinking work-list and a completion proof instead of an argument. Manifest data is chunk-level; module-level attribution needs the sourcemap. TWO FINDINGS THAT SCOPE THE ARC, verified at 138390a -- do not re-derive: (1) the work-list is THREE EDGES, not the 94 grep hits. check-bundle-budget.mjs walks the static graph from budgeted-route entries ONLY, so a lazily-routed page may keep its static import. Home's edges: root.tsx->AuthProvider (useAuth.tsx:3), root.tsx->CityContext.tsx:10, Index->eventRpcs.ts:1. Only two sites repo-wide structurally block a lazy accessor: useOpenRaffles.ts:43 and raffleWaVerify.ts:20, both supabase.rpc.bind(supabase) at module scope. (2) THE HYDRATION INVARIANT: AuthProvider resolves auth in a useEffect, and effects run neither during SSR nor during hydration's first render, so the hydrated tree already renders isLoading:true/user:null identically to the server -- deferring by a dynamic-import tick changes the first client render not at all. NOTHING MAY MOVE AUTH STATE INTO THE FIRST RENDER; that is the one way this arc reproduces the #94-#98 site-wide RR7 #421 bail. The sharper risk lands at P4/P5: detectSessionInUrl:true parses the URL fragment at CLIENT CONSTRUCTION, so deferring construction can silently lose a real OAuth login. Ricky chose P4-then-P5 knowingly and ACCEPTED the prod exposure window between those two merges. Authoring pin: claude-opus-5 / high. P0 is guard-shaped -- it ends in /code-review xhigh, and the matrix says never downshift a guard."
}
