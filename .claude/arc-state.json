{
  "version": 1,
  "arc": "honest-claims",
  "plan": "you-need-to-find-jolly-lark.md",
  "phase": "P1-delete-progressive",
  "set_at": "2026-09-06T19:00:00Z",
  "closed_at": null,
  "required_model": "claude-haiku-4-5",
  "required_effort": "low",
  "scope": [
    "src/modules/event-page/bento/blocks/WeeksLadderBlock.tsx",
    "src/modules/event-page/bento/blocks/__tests__/WeeksLadderBlock.test.tsx",
    "src/pages/seo/BachataInLondon.tsx"
  ],
  "note": "P1 of the honest-claims arc: the site may only assert what the data can evidence. WeeksLadderBlock.tsx:51 hardcoded '{weekCount}-week progressive course' for EVERY format='course' event -- a string with no column behind it. Live canary /event/styling-and-technique-1 (a 3-hour drop-in workshop series) server-rendered '4-week progressive course', VERIFIED on prod before the change. FIX: the word is deleted; the headline is '{weekCount}-week course', arithmetic courseLadderModel already evidences. Plus BachataInLondon.tsx:197, the same unbacked claim in prose -- struck, not replaced. MODEL DEVIATION, declared: matrix pins P1 to claude-haiku-4-5/low; ran on claude-opus-5[1m]. REVIEW, and the reason there were two passes: /code-review low returned (none) but its OWN prompt skips __tests__/, so it reviewed 2 files and 2 of 41 changed lines -- the gate, which is P1's only durable artefact, went unreviewed. That is a coverage gap, not a second round, and Ricky chose to close it. /code-review medium on the spec then ran 17 tool calls and its own independent mutation battery, reproducing M1/M2 rather than trusting mine. Two LOW findings, no correctness bug; both verified against source and both FIXED. F1: SHAPES duplicated the fixtures the tests built inline, so widening a test would have left the gate asserting a shape the suite no longer exercised -- SHAPES is now the SINGLE source and the three tests destructure it. F2 is the one that mattered: the comment claimed the gate holds 'the word may return ONLY from that column, never from this file', and a rendered-markup /progressive/i check cannot enforce that -- it goes green-and-vacuous once the word arrives on a prop, and reds on CORRECT backed copy if a fixture ever carries it, whose cheapest repair is weakening the regex. REDESIGNED as INCLUSION per the reviewer's primary suggestion: a headline() extractor scoped to the claim itself, and ALLOWED_HEADLINE pinning the headline to '{n}-week course [level]' or 'Course finished' and nothing else. An exclusion rule only forbids what its author thought of; '{n}-week intensive course' would have sailed through the old form. The comment now states what the gate CANNOT do -- it makes P10 add its data-sourced case deliberately, it is not proof the value came from the column. BATTERY AGAINST THE FIX (not the defect), 7 mutants, every one reaching its verdict line: M1 original defect KILLED; M2 capitalised finished-branch KILLED; M3 '{n}-week intensive course' KILLED -- the hole the old gate had, and the concrete upgrade; M4 claim appended after the count KILLED by the $ anchor; M5 className smuggle SURVIVED, coverage DELIBERATELY DROPPED because a class name asserts nothing to a reader, declared not overlooked; M6 extractor anchor broken THROWS 'headline div not found' on all three arms, so the fail-loud edge is REACHABLE rather than assumed; M7 CONTROL removing the $ anchor SURVIVES M4's defect, proving the anchor load-bearing. Both files restored byte-identical after every mutant (sha256 checked). CHECKS: spec 6 tests (3 + 3 fanned arms, count read not file count); full suite 101 files / 1540 tests; lint chain 16 of 16 links; tsconfig.app.json 88 PRE-EXISTING tree-wide errors, none in these files. CORRECTION: my first typecheck used tsconfig.json, which is files:[] + references and checks NOTHING -- that green was vacuous and the reviewer caught it. RESIDUALS, scoped not overlooked: BachataInLondon.tsx:73/247/269, LearnBachataLondon.tsx:174 and BachataStyleParties.tsx:64 still say 'progressive' about NAMED courses; the arc's own calibration proved the Sensual Wednesdays three-level claim CORRECT and the auditor wrong, so those are sourced claims for P4/P14. The reviewer noted the page summary and body now disagree and agreed with the scoping. courseLadderModel.ts:9 quotes the old headline inside a historical account of a past refactor -- still true of the era it describes, left alone. safe-write printed PARSE CHECK DID NOT RUN on every write (known worktree defect, arc-worktree-repo-root.md); covered by the typecheck and full suite instead. OPEN: the plan's stated proof -- prod reading '4-week course' -- needs a deploy and is NOT yet verified."
}
