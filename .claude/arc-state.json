{
  "version": 1,
  "arc": "dancer-profile-ownership-key",
  "phase": 3,
  "set_at": "2026-08-10T07:00:00Z",
  "closed_at": "2026-08-10T07:30:00Z",
  "closed_note": "Arc CLOSED at phase 3. Phase 2 merged as 8dbf85c (PR #224, squash, based on main directly, so the stacked-squash trap did not apply); phase 3 is the one-line ratchet lowering that banks its type-error win. closed_at set so arc-checkpoint.mjs stops pinning a model on every prompt, and scope cleared to [] IN THE SAME EDIT: resolveDeclaredScope() in scripts/lib/review-scope.mjs is the sole arc-state consumer that does NOT honor closed_at, so a retained scope array would keep hard-gating pre-ship's declared scope-drift against a closed arc and red-light the next unrelated ship. [] maps to status 'none' and pre-ship reverts to the advisory inferred heuristic. Same remedy as d889831 for the supabase-defer close; the underlying code gap is ledgered in admin docs/open-loops.md 2026-08-07. The phase-3 note below is retained as the arc record, not as a live requirement.",
  "required_model": "claude-opus-5",
  "required_effort": "low",
  "scope": [],
  "note": "PHASE 3 -- ARC CLOSE. Phase 2's one gate red was a FLAKE, not a finding: check-surface-time-agreement reports an RPC error as a reader DISAGREEMENT, and the reader had returned 'search_public_v5: canceling statement due to statement timeout' -- a frontend-only diff cannot move a DB read path. Rerun went green; 17/17 gates. Post-merge check:pr-mergeable swept 7/7 MERGEABLE with 15-17 real gates each, so nothing was orphaned. This phase lowers TYPECHECK_BASELINE 95 -> 91: main MEASURES 91 after 8dbf85c. Measured, not inferred, and measured with the REAL command -- a bare `npx tsc --noEmit` reads a different tsconfig and reported 0, the false-green pre-ship.mjs's own header already documents; the authoritative form is `react-router typegen && tsc -p tsconfig.app.json --noEmit`, which is what the ratchet runs. Lowering while main still measured 95 would have red-lit unrelated ships, which is why the order is verify-then-lower. Carried forward UNFIXED and stated rather than implied, all recorded in 8dbf85c's message: the partner-autosave debounce, the save-ordering ticket and the unmount flush have no automated test (no React Testing Library in the repo), CreateProfile has no end-to-end spec, and EditProfile still sends partner_search_role as `|| null` so that ONE field stays unclearable there while the dashboard clears it."
}
