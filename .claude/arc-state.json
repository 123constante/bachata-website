{
  "version": 1,
  "arc": "supabase-defer",
  "phase": 3,
  "set_at": "2026-08-06T08:30:00Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "scope": [
    ".claude/arc-state.json",
    "src/integrations/supabase/eventRpcs.ts",
    "src/contexts/CityContext.tsx"
  ],
  "note": "PHASE 3 (P3) -- CityContext lazy accessor, following P2's eventRpcs conversion. One call site: isValidCitySlug RPC on line 56, moved from static import to rpcLoose(). Cuts edge 2 (root.tsx:19 -> CityContext no longer pulls vendor-supabase). City validity remains unblocked on first render (still derived synchronously from path/localStorage on lines 33-44; RPC check is only in useEffect). With P2 merged: eventRpcs now uses await getSupabase() (all exports), CityContext now uses rpcLoose(). MEASUREMENT: home 244.7 KB gz / 62 chunks / 3 pullers, event 364.0 KB / 90 chunks / 4 pullers. vs P0 baseline 138390a (244.2/363.5): +0.5/+0.5 KB, +1/+1 chunk(s), 0 puller(s) -- no incremental KB drop because AuthProvider edge 1 still pulls vendor-supabase to first load. Puller count 3: _lazyWithRetry + root + home route (AuthProvider path + eventRpcs path merged into 3 chunks). Weight drops in P4. EDGES CUT AT SOURCE: eventRpcs no longer imports client.ts (P2), CityContext no longer imports client.ts (P3). Still STATIC EDGE: only AuthProvider remains, and CityProvider remains statically imported from root.tsx:19; the RPC call inside is now lazy but the module-level import still exists. P1's hydration invariant unchanged. P4 should diff against 244.7/364.0."
}
