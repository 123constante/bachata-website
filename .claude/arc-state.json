{
  "version": 1,
  "arc": "honest-claims",
  "plan": "you-need-to-find-jolly-lark.md",
  "phase": "P3-strip-experience",
  "set_at": "2026-09-07T14:05:00Z",
  "required_model": "claude-sonnet-5",
  "required_effort": "medium",
  "scope": [
    "src/pages/Experience.tsx",
    "src/components/experience/ExperienceTravel.tsx",
    "src/components/experience/ExperienceMedia.tsx",
    "src/components/experience/ExperienceStage.tsx",
    "src/components/experience/FolderTabs.tsx",
    "src/components/experience/FloatingDock.tsx",
    "src/components/BroadcastTicker.tsx",
    "src/components/TestimonialsSection.tsx",
    "src/components/MobileWidgets.tsx",
    "src/components/AnimatedRoutes.tsx",
    "src/lib/breadcrumbs/siteIa.ts",
    "src/lib/breadcrumbs/buildBreadcrumbs.ts",
    "src/lib/breadcrumbs/__tests__/buildBreadcrumbs.test.ts",
    "vercel.json",
    "eslint.config.js"
  ],
  "note": "P3 of honest-claims. RICKY'S CALL, and it WIDENS the written plan: the plan said keep the tab shell and give each tab one honest sentence; he chose DELETE THE ROUTE ENTIRELY. Rationale is P2's own round-2 record -- every replacement CLAIM generated findings, every DELETION was kept ([[deleting_a_false_claim_beats_replacing_it]]). AUDIT, measured 2026-09-07 on this tree: every claim the plan named is present and confirmed at ExperienceTravel.tsx:46-49 (a named destination trip dated two years stale, presented as upcoming with a pulsing live dot), :54 (a fabricated headcount plus a first-person promise to run the logistics), :69-71 (three unbacked assurances -- instructor quality, a percentage saving on passes, and vetted venues), and ExperienceStage.tsx:58 (a performance guarantee). THE PLAN'S LIST IS INCOMPLETE and the real defect is VOICE, not adjectives: all three tabs are written as though Bachata Calendar OPERATES these services while all three CTAs navigate to DIRECTORY routes -- Travel promises to run the logistics and points at /festivals, Cinema claims a first-person shooting service with a same-day delivery SLA and a booking verb, pointing at /videographers, and Stage asserts a fixed-length programme with styling support, pointing at /choreography. THE CINEMA TAB IS ABSENT FROM THE PLAN'S P3 LIST ENTIRELY and carries the same class of claim. The three hero subtitles in Experience.tsx:22-49 carry it too and are also unlisted. MEASURED FACTS behind the delete-it call: /experience is in NO sitemap and has NO SEO entry (grepped src/lib/seo, scripts, public -- zero hits); all three CTA targets are real directory pages that already exist. Dormant components confirmed ZERO importers each by the CLAUDE.md-mandated grep: BroadcastTicker, TestimonialsSection, MobileWidgets, plus FloatingDock which the plan did not name. TestimonialsSection carries a real EF BB BF BOM at byte 0 and E2 80 99 C6 92 mojibake on line 7 -- both confirmed by od, both closed by deleting the file. THE ONE REAL COUPLING, and it is SEO-visible: siteIa.ts:65 gives festivals `parent: 'experience'`, and breadcrumbs emit Schema.org BreadcrumbList JSON-LD via renderBreadcrumbListJsonLd (PageBreadcrumb.tsx:37). Leaving the parent while deleting the route would advertise a 404 URL to search engines, so the parent MUST go with the route -- deletion is coherent, not optional. Festival chains shorten from 'Experience > Festivals > name' to 'Festivals > name' on /festivals, festival.detail and festival/congress event.detail, which ARE in the sitemap unlike /experience. Declared to Ricky before authoring. resolveEventParent still returns 'festivals' and is unchanged. RouteId = keyof typeof SITE_IA, so dropping the key narrows the union and the compiler catches any straggler. ROUND 1 REVIEW (/code-review high): 3 findings, ALL THREE MINE, all from ONE root cause -- I scoped the reference sweep to scripts/ and .github/ for *.json, so a root-level .js config and vercel.json both sat outside the instrument, and I then wrote the sweep's result up as fact. This is [[state_what_the_instrument_can_see]] and [[coverage_asserted_from_one_side]] together. TWO CLAIMS IN THIS NOTE WERE FALSE WHEN WRITTEN and are struck above rather than reworded: (1) the claim that nothing linked to the route -- vercel.json carried a permanent:true 301 from /:city/experience INTO the route this phase deletes, so the phase turned a live redirect into a soft-404 that browsers cache for ever (NotFound.tsx:16 confirms the catchall answers 200 with a noindex meta, not a real 404); FIXED by repointing it to /festivals and adding the bare /experience rule that never existed, both 301, Ricky's call. (2) the claim that no allowlist named a doomed file -- eslint.config.js's ALLOWED_BARE_AVATAR_FILES named THREE of them (experience/FloatingDock, MobileWidgets, TestimonialsSection) with comments describing them as live, matched by filename.endsWith(), so re-creating any of those paths would silently re-suppress the PersonChip/emitProfileView mandate with no warning; the three entries and their now-false comments are REMOVED. Ricky chose entries-only, not the existence-guard, keeping the phase deletion-shaped. THE REVIEWER ALSO PROVED A NON-FINDING WORTH KEEPING: chunkRatchet in check-bundle-budget.mjs breaks in EITHER direction, so dropping a lazy route could have red-lit with [UNDER]; it did not, because the /experience chunk was dynamic and never in the first-load graph. MODEL DEVIATION, declared: matrix pins P3 to claude-sonnet-5/medium; authored on claude-opus-5[1m]. ALSO DONE THIS SESSION, not part of this diff: main's red contract board was triaged and two of its three reds were ONE defect -- see queued-festival-span-collapsed-by-a-save.md's 2026-09-07 second-instance section. The teacher/DJ red is left alone by Ricky's call. FOURTH TIME IN THIS ARC I QUOTED STRUCK COPY VERBATIM (twice in P2, once in a siteIa comment that reached the SERVER BUNDLE, and once here) -- and here the quotes defeated the very grep written to PROVE the strike, which returned a hit on the correction rather than on the defect. Describe struck copy; never quote it, not even when correcting it. ROUND 2 (/code-review high): 2 findings, NEITHER inside round 1's fixes, so the revert trigger did not fire and the two-round cap closes the phase. F1 (medium, ACCEPTED AND QUEUED by Ricky): deleting this route orphaned /videographers and /choreography -- both still exist, still sit in app/routes/sitemap.tsx and check-gsc's INSPECT_ROUTES, and are now linked from NOWHERE. Not a new class of problem -- /practice-partners was ALREADY orphaned on main with zero linking files -- and there is no directory index to host them (GlobalHeader carries only / and a skip link), so wiring them up means designing a nav surface, i.e. new UI inside a deletion phase. Queued as queued-directory-routes-have-no-nav-surface.md. F2 (low): this note quoted the struck marketing copy verbatim, so a strike-verification grep hit a TRACKED FILE and returned the phase's own record as if it were a surviving defect. FIFTH instance in this arc, and I had predicted it in writing the turn before it landed -- naming a habit is not closing it ([[named_blind_spot_is_not_closed]]). The two offending passages are now DESCRIBED. A REVIEWER CAVEAT THAT DOES NOT HOLD: round 2 reported it could not get a unit-test verdict (all 101 files 'No test suite found'), the environment fault worktree_missing_env_false_red records; the suite was run to completion THREE times in the authoring session at 101 files / 1539 tests exit 0, so the breadcrumb edits ARE execution-verified."
}
