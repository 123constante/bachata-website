{
  "version": 1,
  "arc": "dancer-profile-ownership-key",
  "phase": 2,
  "set_at": "2026-08-09T22:00:00Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "scope": [
    "src/lib/dancerEditorPayloads.ts",
    "src/lib/saveMyDancerProfile.ts",
    "src/components/profile/DancerDashboard.tsx",
    "src/pages/CreateProfile.tsx",
    "src/pages/EditProfile.tsx",
    "tests/dancerEditorPayloads.test.ts",
    "tests/e2e/dancer-dashboard-concept-b-smoke.spec.ts",
    ".claude/arc-state.json"
  ],
  "note": "PHASE 2 -- the bounded remainder of phase 1: DancerDashboard and CreateProfile still wrote direct-to-table, so both editor forms were still 42501 for every user. Pin: claude-opus-5 / high, /code-review xhigh with an EXPLICIT target. NOT A MIGRATION. Three review rounds failed to converge on these two forms in phase 1 because the fix turns a large body of DEAD code live, so the mapping now lives in ONE pure, tested module (src/lib/dancerEditorPayloads.ts) rather than inline in two 1,600/1,800-line screens. MEASURED CONTRACT FACTS read off pg_get_functiondef and pg_constraint this session, which CORRECT phase 1's notes: (a) 'the RPC cannot clear a field' is true ONLY of the ten identity scalars (COALESCE(NULLIF(x,''), dp.x)). The dancer_details sidecar arm has NO NULLIF -- it is COALESCE(EXCLUDED.x, existing.x) over `->>` -- so '' IS a real value there and CLEARS partner_details and partner_search_role, and [] clears the lists. Ricky chose (2026-08-09) to adopt clearing on all three self-serve editors, which is why EditProfile.tsx:224 changed too. (b) LANDMINE: the three sidecar list fields are read as CASE WHEN details ? 'k' THEN ARRAY(SELECT jsonb_array_elements_text(...)). A key PRESENT with a JSON null therefore hits jsonb_array_elements_text('null'), a HARD ERROR, not a no-op -- and both screens sent exactly that via `x.length ? x : null`. Arrays go as ARRAYS, always. (c) dancer_profiles_dance_role_check admits only Leader / Follower / 'Lead and Follow', and normalize_dance_role maps our badge value 'Both' onto the third. Both screens' local normalizePartnerRole had NO 'lead and follow' case, so the stored form of Both read back as 'no role selected' and the next save cleared a role the user had set -- unreachable while the writes failed, live the moment they work. One codec now, dancerRoleFromStored, mirroring the SQL helper's own spellings. (d) dancer_profiles_dance_started_year_check bounds the year to 1950..current year; EXPERIENCE_LEVEL_YEARS is a DURATION, so the wizard's `dance_started_year: EXPERIENCE_LEVEL_YEARS[k]` meant 'Intermediate' = the year 4 AD and would have 23514'd. DECISIONS: (1) saveEditor's activeEditor === 'partner' branch was UNREACHABLE (openEditor routes that section to the inline editor and never sets activeEditor) and is deleted rather than ported -- a second unreachable write path is where the next silent divergence grows. (2) The dashboard repaints from the row the function RETURNS, not from a locally assembled nextLocalProfile, which is what used to write the four phantom fields into local state and leave the city label stale. (3) CreateProfile's didUpdate is decided by a READ before the save, not by the prefill effect, which has not resolved on the pre-auth path. (4) isSubmitting is reset in a finally, but NOT on the navigating path: re-enabling useUnsavedChangesGuard for even one render pushes a junk history entry over the page being left. PROVEN BOTH DIRECTIONS: 9 mutations, 9 reds -- 7 against the payload builders (unit) and 2 against the dashboard/e2e (hydration wipe, partner_details double-encode). NOT COVERED, stated rather than implied: the 600ms debounce, the save-ordering ticket and the unmount flush have no automated test, because the repo carries no React Testing Library and adding component-test infrastructure inside this arc would be a large unreviewed surface. NAMED DEBT, not fixed: EditProfile still sends partner_search_role as `|| null`, so that ONE field remains unclearable there while the dashboard clears it. ROUND 1 REVIEW FOLDED (14 findings, all as edits into the same commit): the wizard prefill became a field-wise MERGE with no completeness gate and the pre-auth auto-submit now AWAITS it (an unhydrated wizard submit cleared the stored sidecar of 1 live row, measured); partner autosaves are SERIALISED behind one in-flight chain, since the ticket ordered repaints but not writes; openEditor flushes and carries the pending debounced form rather than reverting it; the tile toggle rolls its optimistic state back on failure; partner_search_role hydrates RAW because it is free text with no CHECK and the role codec would map an unknown value to '' -- which now clears; the identity form seeds photo_url from avatar_url (the WRITABLE column) rather than from photo_url (its MIRROR, stored here as a display value) -- note the round-1 rationale claiming getPhotoUrl rewrites storage keys onto an events bucket was FALSE and is corrected in the code, the e2e fixture and here; the section dispatch is exhaustive; the level lookup is own-key guarded (a prototype key made the year NaN, dropped in silence); the role codec splits on the whitespace CLASS to match normalize_dance_role; the leftover phantom partner_role field is gone; two vacuous isInlinePartnerEditorOpen guards removed; the e2e RPC mock now returns dancer_profiles columns only, since the live function cannot return the cities join. saveMyDancerProfile's header gained dance_started_year: the clearable/unclearable split runs by TYPE, not by identity-vs-sidecar. The merge was EXTRACTED into mergeStoredDancerIntoWizardForm so the highest-severity fix could be tested at all. Round-1 fold proof: 20 mutations, 20 red -- but only after one of them came back GREEN and exposed a vacuous case of my own (the flush assertion polled the last payload, so when its badge click silently missed the target the debounce timer's own save satisfied it; it now waits on the payload COUNT first). STILL UNCOVERED, stated rather than implied: F4's write ordering (needs concurrency control the mock does not have) and the CreateProfile screen end-to-end (no spec, and no React Testing Library in the repo). Separately diagnosed and fixed here: the new pure unit file imported @/lib/saveMyDancerProfile, which constructs the REAL Supabase client at import time, and that live client in an extra worker reproducibly tipped three timing-sensitive edge-TTL tests over in PARALLEL runs while passing in isolation and sequentially -- mocking the client made 4/4 parallel runs green. ROUND 2 FOLDED (13 findings, all as edits, same commit; this was the last round per the stopping rule). The round-1 wipe fix had reintroduced the wipe through its ERROR path: loadExisting discarded the read error, so a transient 5xx resolved the prefill promise and the auto-submit sent a blank wizard. It now throws, unlatches, and BOTH submit paths refuse to write when the prefill failed. The merge is re-keyed on react-hook-form dirtyFields, because emptiness cannot distinguish 'unanswered' from 'deliberately off' -- and the localStorage draft restore had to gain shouldDirty, or the merge would have treated the whole restored draft as untouched and overwritten it. reset now passes keepDirty so useUnsavedChangesGuard stays armed. Partner autosave: the catch arm is ticket-guarded (a stale failure rolled back state a later save had committed) and toasts are suppressed after unmount. The partner handlers compute from a mirrored ref, since batching made the closure form silently revert a tap in the DATABASE. normalizeSocialUrl no longer treats a dot as a URL, so '@ana.dance' stops becoming https://ana.dance. Two vacuous test shapes of my own were fixed: the rollback assertion now pins an attempted save and a visible failure, and the avatar fixture now differs from its mirror so it tests the hazard it names. 25 mutations across both rounds, all red."
}
