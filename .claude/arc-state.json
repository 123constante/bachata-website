{
  "version": 1,
  "arc": "honest-claims",
  "plan": "you-need-to-find-jolly-lark.md",
  "phase": "P2-strip-discounts",
  "set_at": "2026-09-07T07:11:00Z",
  "required_model": "claude-sonnet-5",
  "required_effort": "medium",
  "scope": [
    "src/pages/Discounts.tsx",
    "src/components/SavingsCalculator.tsx",
    "src/components/RecentSignupsTicker.tsx",
    "src/components/TonightSavingsAlert.tsx",
    "src/components/LiveDiscounts.tsx",
    "src/components/CommunitySpotlight.tsx",
    "src/components/DiscountPartners.tsx",
    "src/lib/seo/buildSeoForRoute.ts",
    "src/hooks/useEvents.tsx",
    "src/lib/breadcrumbs/siteIa.ts"
  ],
  "note": "P2 of honest-claims: /discounts keeps its route and is stripped to stated INTENT, because there is no product behind it (no payment processor, no membership/discount/partner/testimonial table, 19 auth users). P1 CLOSED and merged (fc7ee099, PR #365); its prod proof is in the handover. STEP-0 SPIKE, measured 2026-09-07 in C:/dev/Website-wt-planhygiene against prod stsdtacfauprzrdebmzg -- CONFIRMED: all six discounts components are imported by exactly one file (src/pages/Discounts.tsx); get_discount_partners_with_next_event has NO discount predicate (pg_get_functiondef read live -- it returns active organisers joined to their next active future event, city-filtered), so 'Partnering with London's Best' is a false claim of commercial association; it returns 0 rows for london-gb AND for a NULL city; dancer_profiles = 151, so CommunitySpotlight's '2,600+' is fabricated. The empty-partners MECHANISM, which the plan did not name: 7 distinct organisers sit on the 7 future active events and NONE of them is is_active, while the 2 active organiser_profiles have no future event -- so the section re-arms the moment an is_active flag flips, which is exactly why the title has to be honest now rather than deleted. TWO DISAGREEMENTS WITH THE PLAN, reported not built on: (1) the plan says scripts/rpc-typing-allowlist.json 'needs its key updated' -- the key is the FILE PATH src/components/DiscountPartners.tsx, which this phase KEEPS, so no allowlist edit is needed unless the file is renamed; (2) the plan says CommunitySpotlight can be kept because it 'reads real data' -- it reads real avatars AND hardcodes '2,600+ active dancers', the arc's own fabrication list item, so keeping it requires that literal to go. NOT IN THE PLAN's P2 list, found by reading the page: the hero itself asserts the product ('VIP Membership', 'GBP 20/month. Best discounts in London. Guest list at every event.') plus three heroWidgets; '500+ VIP members' at line 195; 'Cancel anytime. No commitment.' at line 216; and handleJoinClick is the only link into /auth, which login-is-PARKED says is live-but-deliberately-unlinked. ROUND 1 REVIEW (/code-review high, target arc/honest-claims-p2): 6 findings, Ricky chose \"all six now\" -- ALL SIX FIXED IN THE WORKING TREE, none deferred to a follow-up commit. Stopping rule stated OUT LOUD before the round: user-facing, so two rounds maximum; if round 2 finds defects INSIDE these fixes, revert and queue rather than open round 3. THE SEVERE ONE (F1) was self-inflicted by this phase: DiscountPartners' new empty state said \"Nobody has an upcoming event flagged right now\" when the query had NEVER RUN -- enabled:!!citySlug, and bare /discounts reads citySlug from localStorage only, so a first-time visitor and the prerender pass both had null. The old code returned null on that branch, so the strip INTRODUCED the unbacked claim the arc exists to delete. FIX: FALLBACK_CITY_SLUG='london-gb' (only London is active in the catalog), query always enabled, so the empty state is now an observation backed by a fetch -- and the STEP-0 spike measured the RPC returning 0 rows for london-gb, so what it renders today is true. F2: isError was never destructured, so an RPC 500 or revoked anon grant was reported to the reader as a fact about organiser activity; it now has its own branch. Sentry already receives it via the QueryClient global onError (App.tsx:37), so that branch only owns what the reader is told. F3 (dancer count) RESOLVED BY RICKY'S CALL, not as reviewed: the reviewer proposed matching Dancers.tsx:78's is_active filter, but Ricky's WhatsApp community is 3,500 against 151 dancer_profiles rows, so the DB count was never the number worth showing. The count query is DELETED; the figure is now WHATSAPP_COMMUNITY_SIZE='3,500+' labelled \"dancers in the London WhatsApp community\" -- a COMMUNITY claim, never a platform one. DECLARED HONESTLY IN THE COMMENT: this is the same SHAPE as the fabricated literal it replaces and differs only in being true on the day it was written; P7 must give it an evidence source or strike it. The avatar strip DOES take the is_active filter, so it cannot show a face /dancers omits. F4: the try/catch could never fire because supabase's builder RESOLVES with {data,error} rather than rejecting; the error is now read off the result, sent via captureException (matching DancerDashboard.tsx:437), and sets avatarsFailed so a failure stops the skeleton instead of pulsing it for ever. F5: discounts.title was the bare `Bachata Organisers`, colliding with the organisers spec and the only spec in the block dropping city interpolation, including on the live /city/:slug/discounts route; now `Bachata Organisers With Upcoming Events in ${city}`. Nothing asserts these titles -- check-gsc.mjs lists paths only, zero title assertions -- verified before changing. F6: siteIa.ts label 'Discounts' -> 'Active Organisers'. siteIa.ts was ABSENT from this phase's scope array and has been ADDED, declared not smuggled. FOUND WHILE VERIFYING, not by the review: my own fix comment quoted the old fabricated figure verbatim, and it survived into the built .js.map -- a comment carrying the exact literal a fabrication guard hunts for would trip that guard from the very code that removed the claim. Reworded; the literal is gone from the whole build tree. CHECKS AFTER THE FIXES: tsc -p tsconfig.app.json = 88 errors, EXACTLY the pre-fix baseline for this worktree, zero new; the one error in CommunitySpotlight.tsx is the SAME pre-existing TS2345, relocated by an added comment block, and is left alone as out of scope. Full suite 101 files / 1540 tests, exit 0. Lint chain 16 of 16 links. Build exit 0. Fabrication sweep over the WHOLE build tree: 2,600+ / 500+ VIP / VIP Membership / Best discounts in London / Cancel anytime / Partnering with London / Sarah M. / membershipCost / dancers on the platform ALL ZERO, with a CONTROL proving the instrument reaches the right files (3,500+, the WhatsApp label and the new title each hit 4 files). A TRANSIENT, NOT A DEFECT: one run red tests/latestEvents.contract.test.ts with 57014 canceling statement due to statement timeout, on get_latest_events_v2, which this diff does not touch. NOT called flake from n=1 and NOT judged by re-running the spec alone (that changes concurrency, the trap this repo's own handover recorded): the FULL suite was re-run at the SAME concurrency and came back 1540/1540 exit 0. safe-write printed PARSE CHECK DID NOT RUN on every write (known worktree defect, arc-worktree-repo-root.md); covered by the typecheck, the full suite and a successful build. MODEL DEVIATION, declared: matrix pins P2 to claude-sonnet-5/medium; authored on claude-opus-5[1m]. The session's arc-checkpoint hook injected the P1/haiku pin because main's arc-state.json still reads P1 -- this phase's state rides on this unpushed branch and the pin self-corrects once it merges. OPEN: round 2 has NOT run; these fixes are unreviewed code. Branch is rebased onto eec8dcd, still UNPUSHED, no PR. QUEUED, out of scope: MobileWidgets.tsx:269 still says \"Join 500+ dancers\" against 151 profiles, but the file has only a self-export, no importer, and does not reach the bundle -- dead code carrying a fabricated claim, for P4. ROUND 2 (/code-review high): 6 findings, and THREE SAT INSIDE ROUND 1's OWN FIXES (the empty state, the isError branch, my SEO comment) -- the revert-and-queue signal pre-committed before round 1. The reviewer flagged the rule as fired independently of me. RULE HONOURED: no round 3 was opened. DECISION (Ricky answered \"you decide\" on all three forks): SPLIT SCOPE TO DELETION-ONLY rather than revert the phase, because reverting 7df59c8 would put the fabricated VIP membership surface back on prod -- the opposite of the arc's purpose. Every replacement CLAIM is deleted; every deletion is kept. THE FINDING THAT MATTERED (R2-F1, HIGH): the empty state was false regardless of whether the query ran, so round 1's fix made it RUN AND STILL LIE. Measured live: the RPC gates on op.is_active = true and returns 0 rows for london-gb while SEVEN organisers have upcoming active events and are simply not flagged -- emptiness is an admin flag state, not absence of activity. DELETED: the section now returns null on zero rows. That single deletion also kills R2-F2 (the isError branch replaced a rendered grid with an error box on a background refetch error, because query-core retains `data` on a refetch error while flipping status to 'error'); with no isError branch, cached rows keep rendering and a failure with nothing cached shows nothing. R2-F3: my round-1 comment claimed the title now interpolates the city, which NEVER HAPPENS -- Discounts.tsx passes no SeoContext so city() always yields the default; the interpolation is removed rather than half-fixed, and the comment now records that the DESCRIPTION carries the same limit and is left alone only because London is the one active city. R2-F4: hero's hardcoded city struck (same component serves /city/:slug/discounts) along with the trailing activity clause, which could announce a section that now renders nothing. R2-F5: the pulsing avatar skeleton is DELETED rather than given a third state -- it conflated fetching, fetched-nothing and failed, so two of three pulsed for ever; avatarsFailed went with it. R2-F6: the london-gb fallback and its comment (which cited a prerender pass that does not exist -- /discounts is not in react-router.config.ts's prerender list) are reverted entirely; the query is back to enabled:!!citySlug. ALL SIX ADDRESSED BY DELETION, none by redrafting. TWICE IN ONE SESSION I QUOTED STRUCK COPY IN A COMMENT AND IT SURVIVED INTO THE BUILD -- first the fabricated count, then the activity clause, the second one reaching the SERVER BUNDLE and not merely a sourcemap. A comment carrying the exact literal a fabrication guard hunts for would red that guard from the very code that removed the claim. Struck copy is now DESCRIBED, never quoted, and the rule is written into the comment that learned it. CHECKS ON THE SHIPPING STATE: tsc 88 = baseline, zero new; suite 101 files / 1540 tests exit 0; lint 16 of 16; build exit 0; sweep of eleven struck strings over the whole build tree ALL ZERO with four controls proving the instrument reaches the files. LOAD, NOT THE DIFF: across four full-suite runs, two DIFFERENT specs each timed out ONCE (latestEvents.contract 57014 statement timeout; festivalLoaderEndedShare 5000ms) and each passed on re-run at the SAME concurrency -- never judged by running a spec alone, which changes concurrency. QUEUED, NOT FIXED: (1) the is_active flag question -- 7 organisers with real upcoming events are unflagged, so either the flags are stale (admin data fix) or the RPC's gate is wrong (admin migration); PARKED deliberately, and the Website side is now honest under either answer; (2) SeoContext is not threaded through Discounts.tsx, so the description cannot name a city truthfully; (3) with the fallback reverted, a first-time visitor with no stored city sees no section at all -- a functional gap, no longer a false claim; (4) MobileWidgets.tsx:269 \"Join 500+ dancers\" against 151 profiles, dead code with no importer, for P4; (5) the pre-existing CommunitySpotlight TS2345, part of the 88. STAMP-REPAIR PASS 2026-09-07, authored on claude-opus-5[1m] -- review sessions are exempt from this phase's sonnet pin by the arc-checkpoint's own SCOPE line, declared not smuggled. WHY A THIRD ROUND RAN AT ALL: the previous session's .claude/.review-stamp.json recorded six findings with hashes:{} and deletions:[], provenance branch main / head eec8dcd, so every P2 file read as never-reviewed and ship-gate exited 1. The RECEIPT was corrupt, not the code; writeStamp overwrites wholesale, so one fresh review mints a valid one. Stopping rule stated OUT LOUD before the round: ONE round, findings sitting inside already-adjudicated deletions get noted and shipped or reverted, never redrafted. SCOPE WIDENED HERE, declared: tests/festivalLoaderEndedShare.test.ts is b3ff6c6's file from PR #370 and entered P2's ship diff through the REBASE onto it -- it is not P2's work. scopeDrift over the branch file list returns ok:false severity:error and pre-ship --dry-run prints [FAIL] scope-drift; BOTH WERE RUN rather than taken from the review, because a reviewer's claim about a gate is still a claim. RESOLVED THE OTHER WAY IN THE END, and the entry is GONE from the array again -- recorded because the widening above is now history, not current state: #370 was re-run (its unit gate went green on a rested DB, confirming the eventViewCompat timeout was saturation), then squash-merged as e376d03, and P2 was rebased with `git rebase --onto origin/main b3ff6c6`, which drops that commit DETERMINISTICALLY rather than trusting patch-id detection to notice a squash. P2 is now the single commit 7a4777e on main and its ship diff is its own 11 files, so the test file is not in scope and must not be declared. #370's contract-check reds were NOT a reason to hold it: main fails the SAME checks with the SAME numbers (35 teacher / 7 DJ against a 36/6 baseline; P5 materialised drift_count=3, time_drift=3) across THREE different SHAs, which is data-not-code in its cross-sha form. Those three are a standing DB problem that belongs to nobody's diff and is still open. ROUND 3 (/code-review high, target arc/honest-claims-p2): 6 findings, ONE live-blocking (that drift). F3: the indexed title still asserts organisers with upcoming events while the body now returns null on 0 rows -- the LAST surviving copy of this phase's target claim, and the reviewer is right that it is in-scope. SHIPPED AS-IS BY RICKY'S CALL, queued for P7: this is the THIRD draft of that one string, P7 already owns the SeoContext threading it depends on, and the revert-rather-than-redraft rule binds harder than the claim is severe -- a category label over an empty list is a far weaker falsehood than the commercial-association claim this phase deleted. F2 (enabled:!!citySlug, so a first-time visitor with no stored city sees no section) was ALREADY queued as residual (3) above; the review rediscovered it, it is not new. F5 (the WhatsApp community label) was already Ricky's call and deferred to P7, but the review added a NEW angle worth carrying: the avatar query has no city filter, so the newest active dancer_profiles row from any city renders under a London label. F4 (formatDate's isToday compares day and month but NOT year, and returns before the precise toDateString check, so 2027-09-07 renders as Today on 2026-09-07) and F6 (partner.city || 'London' invents a city for a NULL city_id) are PRE-EXISTING, untouched by this diff, and QUEUED not fixed -- P2's scope is deletion-only and widening it to carry drive-by repairs is how a phase stops being reviewable. CONTEXT ON #370, whose board is red and NOT from its diff: six contract checks exit 2 on canceling statement due to statement timeout, one exits 1 on teacher/DJ baseline drift (35 teacher / 7 DJ against a 36/6 baseline), and eventViewCompat.contract times out at 5000ms with 1538 of 1540 passing. A test-only commit cannot move assignment counts or DB latency; this is the live-DB saturation pattern the memory already names, not a defect in #370."
}