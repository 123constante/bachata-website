{
  "version": 1,
  "arc": "series-termination",
  "plan": "arc-series-termination.md",
  "phase": "P4",
  "set_at": "2026-09-02T08:10:00Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "scope": [
    "src/modules/event-page/bento/lifecycleBanner.ts",
    "src/modules/event-page/bento/EventEndedBanner.tsx",
    "src/modules/event-page/bento/EventEndedRecord.tsx",
    "src/modules/event-page/bento/utils/endedRun.ts",
    "src/modules/event-page/bento/blocks/DateBlock.tsx",
    "src/modules/event-page/bento/BentoPage.tsx",
    "src/modules/event-page/buildEventPageModel.ts",
    "src/modules/event-page/useEventPageQuery.ts",
    "src/modules/event-page/types.ts",
    "src/modules/event-page/sections/MoreEventsSection.tsx",
    "src/modules/event-page/bento/EventCancelledBanner.tsx",
    "src/modules/event-page/bento/EventPausedBanner.tsx",
    "src/modules/event-page/__tests__/buildEventPageModel.ended.test.ts",
    "src/modules/event-page/bento/__tests__/lifecycleBanner.test.ts",
    "src/modules/event-page/bento/utils/__tests__/endedRun.test.ts",
    "src/pages/FestivalDetail.tsx",
    "src/modules/event-page/endedShareDescription.ts",
    "tests/endedShareDescription.test.ts",
    "src/lib/buildEventJsonLd.ts",
    "src/lib/buildEventJsonLd.stress.test.ts",
    "app/routes/event.tsx",
    "tests/sitemapEdgeTtl.test.ts",
    ".gitignore"
  ],
  "note": "P4b BUILT 2026-09-02, NOT PUSHED. THE PUSH IS THE GATED STEP (Vercel auto-deploys main). SUPERSEDED: the old note here said P4a would flip the occurrence ORDER BY to descending for ended series. That draft was REVERTED at review. What shipped is P4a (event.ended_on, ledger 20260902140000) plus P4c (event.ran_from + occurrence_effective resolved from the stored ended_on, ledger 20260902230000), with the 52-row window DELIBERATELY UNTOUCHED -- a capped array cannot carry both ends of a run. Both ends are SCALARS; never re-derive either from snapshot.occurrences. CONSEQUENCE: occurrence_effective for an ended series is now the LAST night, so the date tile shows the closing date, not the first-ever one. The DESIGN-CLOSED row saying Date tile = First night described pre-P4c behaviour. DESIGN CLOSED (claude-fable-5 step), unchanged: Approach B Archive Entry; sticky slate banner, never the red cancelled shape; ENDED cap + desaturated date tile; raffle and tickets suppressed on isEnded (not on past); banner precedence ended STACKS above cancelled (selectLifecycleBanners returns a LIST) while cancelled still replaces paused; forward door = MoreEventsSection with a new blocks prop, door [organiser] + its others-fallback, bottom [thisWeek, calendarPill]; og:description only (the R2-baked flyer image is OUT and queued); JSON-LD dates unchanged on purpose. W11 fix (drop paused from the admin editor publicUrl condition) is admin-side and NOT part of this deploy. TWO FILES IN THE SCOPE ARRAY ARE NOT P4b RENDER CODE, both carried because they were blocking this ship's gate and neither is this arc's doing: (1) tests/sitemapEdgeTtl.test.ts -- its no-op query-builder mock listed select/eq/neq/order/limit, and #329 (3cfa9e5) added .not(...NOT_DEACTIVATED) to the loader's fetchDancerProfiles, which is not flag-gated; the missing method threw, the loader's catch 500'd, and test:unit:timing has been red on main since. One word added to the list, proved in both directions. (2) .gitignore -- 14 untracked root-level home-map-*-mockup.html / nav-loading-*.html scratch files from another session sat in this shared checkout and fatally scope-drifted the ship. The block they belong in already ignores this exact class by pattern (/*-mockups.html, /tmp_*.html, ...); these slipped it on naming alone, so two anchored patterns were added rather than anyone's files being moved or deleted. (3) src/lib/buildEventJsonLd.ts + its stress test -- review round 2 found the page's own JSON-LD declaring an ended series as EventScheduled with an InStock offer and the stored sales description, i.e. telling Googlebot the opposite of what the banner and og:description say. The offers suppression had to live IN the builder: both of its branches assert availability, including a fallback Offer that fires when there are no tickets at all, so passing an empty array from the call site still declared one. eventStatus was deliberately NOT changed -- schema.org has no finished value and a run that reached its last night was not cancelled, so saying EventCancelled would be a new false statement."
}
