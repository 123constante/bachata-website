{
  "version": 1,
  "arc": "supabase-defer",
  "phase": 2,
  "set_at": "2026-08-06T07:20:00Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "scope": [
    ".claude/arc-state.json",
    "src/integrations/supabase/eventRpcs.ts"
  ],
  "note": "PHASE 2 -- the data path. All four exported readers in eventRpcs.ts (getCalendarEvents, getEventPageSnapshot, getLatestEvents, getMapEvents) were already async, so each gained one line -- `const supabase = await getSupabase();` -- and the module's static `import { supabase } from './client'` is gone. Nothing else in the file touches the client; parseCalendarEventRow is pure. MEASUREMENT, and this is the phase's real finding: the weight did NOT move and NO puller left. A/B built in one worktree on one toolchain, main(5071590) vs main+P2: home 244.3 KB/61 chunks/3 pullers -> 244.7/62/3, and event 363.8/89/4 -> 364.0/90/4. That is +0.4 KB and +1 chunk for zero pullers -- P2 standalone makes first load marginally WORSE. WHY, so no later phase re-derives it: app/routes/home.tsx does not reach vendor-supabase only through eventRpcs. It also imports createQueryClient from '@/App' (-> AuthProvider -> useAuth -> client, edge 1) and '@/pages/Index' (edge 2 class). While ANY static importer of client.ts survives, client.ts stays statically reachable, Rollup keeps it inside the first-load _lazyWithRetry chunk, and the new `await import()` only adds a chunk boundary on top of a module that is being shipped anyway. The dynamic edge buys nothing until it is the LAST edge. Corollary for sequencing: the arc's payoff is not incremental -- it arrives when P3 and P4 land, and no phase before that should be judged on KB. Judge P2 on the puller SET's composition changing at the source level, which it did: eventRpcs is no longer a path to client.ts. EDGE 3 IS CUT AT SOURCE but home.tsx remains a puller for the two reasons above; do not read its continued presence in the list as P2 having failed. SSR VERIFIED FROM THE ARTEFACT, not from a passing test, because this boundary fails silently: served the production build via scripts/_serve-build.mjs and fetched '/' -- HTTP 200, 234,884 bytes, JSON-LD carries 25 ListItem entries and 25 Event entries, and dehydratedState is present and POPULATED (38 seoEventLinks, London city payload). The loaders already awaited these functions, so adding one more await before the rpc call changed nothing observable server-side. Suite: 769 passed / 1 skipped, 60 files. Guards green: mojibake, lint:architecture, route-boundaries, rpc-typing. tsc --noEmit clean. CARRIED FORWARD, not new: RaffleEntryDialog.tsx still holds a static `import { supabase } from '@/lib/supabase'` and is in the event page's first-load graph -- recorded in P1, out of scope here. HYDRATION INVARIANT still holds: every call site is an async queryFn or loader, none runs during render, so the first client render is unchanged."
}
