{
  "version": 1,
  "arc": "honest-claims",
  "plan": "you-need-to-find-jolly-lark.md",
  "phase": "P5a-jsonld-honesty",
  "set_at": "2026-09-08T09:37:08Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "review_effort": "xhigh",
  "scope": [
    "src/lib/buildEventJsonLd.ts",
    "src/lib/buildEventJsonLd.stress.test.ts",
    "src/lib/buildEventListJsonLd.ts",
    "tests/buildEventListJsonLd.test.ts",
    "src/modules/event-page/bento/BentoPage.tsx",
    "src/pages/FestivalDetail.tsx"
  ],
  "note": "P5a of honest-claims -- JSON-LD honesty, TWICE SPLIT, shipped as deletions. AUDIT (2026-09-08, this tree + live DB + deployed prod): prod / and /city/london-gb each served 25 x 'Bachata Artists', 25 x 'Bachata Calendar' as organizer, 25 x InStock, 25 x a generated description, from ONE ItemList. THE PLAN UNDER-COUNTED THREE TIMES (P4's pattern): it named one test to invert where five pinned removed behaviour; it missed a SECOND 'Bachata Calendar' fallback at BOTH detail call sites; and its 'delete both branches' for offers read two ways. Both widenings went to Ricky as clickable questions. DATA on the 25 emitted rows: all 25 carry a real primary_organiser_name across 19 distinct organisers, so nothing loses its organizer -- it stops being us; 0 of 25 carry meta_data.description; 0 are cancelled, so the is_cancelled fix is correct but NOT verifiable from today's deployed output -- stated, not claimed. ROUND 1 (/code-review xhigh): 14 findings, one MERGE-BLOCKING and verified at source -- check-seo.mjs:317 hard-fails 'Event JSON-LD missing offers' and seo-check.yml's seo-preview runs on pull_request, so THE GUARD PINS THE FABRICATION AS CONTRACT exactly as stress.test.ts:93 pinned the invented performer. The plan anticipated that trap for the test, not the guard. The location null-return was coupled to the same guard's MIN_EVENT_PAGES=3 floor. SPLIT #1 (Ricky): ship only what the gate does not assert; offers and location left BYTE-IDENTICAL to main (grep-verified against main for InStock / 'United Kingdom' / placeName / return null: no hits). That RETIRED findings 1,2,3,8,11,12 by deleting the null return, the new JsonLdScript component and the renderEventJsonLd signature change rather than shipping them. ROUND 2 (/code-review xhigh): 11 findings, and FOUR SAT INSIDE ROUND 1'S FIXES, three confirmed at source rather than on the reviewer's word. (a) Exporting renderWidgetHtml so a test could reach it ships a SERVER-ONLY renderer to the browser: @react-router/dev strips only SERVER_ONLY_ROUTE_EXPORTS = [loader, action, middleware, headers] (vite.js:2823), and that route's client chunk is literally 1 BYTE today because loader was its only export -- no perf-budgets.json row covers the route, so no guard would have caught it. (b) My .find() over organisers can credit a NON-PRIMARY organiser: buildEventPageModel.ts:119 defines the primary as organisers[0], so the page body and the structured data would name different businesses -- a FRESH misattribution introduced by the arc that exists to remove misattribution. (c) The new spec constructs a real Supabase client at import, which reds as 'N failed FILES | 0 failed TESTS' in any tree with no .env -- the defect TWO of my own memories already warn about. SPLIT #2, the pre-committed stop, Ricky's call: REVERT TO DELETIONS. The whole widget half went back to main (byte-identical) and BentoPage returned to a row-zero test that agrees with buildEventPageModel. AGAIN every REPLACEMENT I authored drew a finding and every pure DELETION drew none -- the ninth and tenth instances of [[deleting_a_false_claim_beats_replacing_it]], and the second consecutive phase where the stop fired. FOUND WHILE FIXING A FINDING, worth more than the finding: tests/embedCalendarTemplate.test.ts covers api/embed/_template.ts, a STALE COPY whose ONLY importer is that test, so the live widget had ZERO unit coverage behind a green 9-test suite -- and my first fix added assertions to the dead suite, where they passed against the stale renderer. I ALSO PIPED A SUITE RUN THROUGH `tail` AND LOST THE FAILURE DETAIL, the third instance in this arc and explicitly warned about in P4's handover; the rerun captured unpiped. VERIFICATION: unit suite 101 files / 1549 tests ALL GREEN, up exactly 10 from P4's 1539 (+2 stress, +8 list); an earlier run of the IDENTICAL tree showed 8 transient failures in live-DB and timing-sensitive specs (latestEvents.contract opens a real client) during the morning's 57014 statement-timeout storm -- the same tree passing is the evidence it was not the diff. Typecheck 88 against the post-typegen baseline of 88, zero new, none in touched files. QUEUED: queued-p5b-offers-location-seo-guard.md (incl. the review's better location design -- schema.org Event.location accepts a bare PostalAddress, flagged to verify against GOOGLE's requirements and not only schema.org's range), queued-embed-widget-dead-template-copy.md (rewritten to carry the ORDER that works and the 1-byte-chunk trap), queued-jsonld-residual-fabrications.md (hardcoded GB on non-UK festivals, the 'London' locality default, uncapitalised Place.name, the festival organiser.href that is never populated, the dead renderEventJsonLd export, and the unescaped </script> exposure across TWELVE JSON-LD sinks -- reachability measured: none of the twelve escapes anything, and Index.tsx:202 inlines organiser-written events.name). NEXT, Ricky's calls: the escaping PR first, then P5b; prod-smoke false reds ride with #330, with the caveat that #330 classifies RPC error codes while prod-smoke sees browser-side 500s. MODEL: claude-opus-5[1m], matching the matrix for BUILD-non-trivial."
}
