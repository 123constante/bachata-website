{
  "version": 1,
  "arc": "operating-model-v2",
  "phase": 11.4,
  "set_at": "2026-08-05T00:05:00Z",
  "scope": [
    ".claude/arc-state.json",
    ".github/workflows/db-contract-check.yml",
    ".github/workflows/synthetic-ssr-monitor.yml"
  ],
  "note": "PHASE 11.4 -- the last two workflows still pinned to Node 20, found by diagnosing why Dependabot PR #187 (53 npm bumps) went red. NOT a Dependabot problem and not a cosmetic version drift: @supabase/* 2.111 declares engines.node >= 22.0.0, and @supabase/realtime-js needs the NATIVE WebSocket global that Node 22 provides and Node 20 does not. On 20, EVERY DB check in db-contract-check.yml dies with 'Node.js detected but native WebSocket not found' -- that file carries 132 Supabase references, so the entire contract suite would have gone dark the moment the bump landed. A CORRECTION WORTH RECORDING, because the first diagnosis was WRONG and would have shipped a fix that fixed nothing. I read the package.json diff, saw a new `engines.node >= 20.9.0`, and concluded that `node-version: 20` was resolving BELOW 20.9. Then I checked what setup-node actually resolved in the failing run: v20.20.2, which SATISFIES 20.9. The real constraint was one level down, in the @supabase SUB-packages (auth-js, functions-js, postgrest-js, realtime-js, storage-js, all 2.111), each demanding >= 22.0.0, visible only in the npm EBADENGINE warnings. The top-level engines field was a red herring. Read the RESOLVED version and the transitive constraints out of the actual run log; do not infer either from a diff. Both files moved to 22, matching the other 20 workflows, so no `node-version: 20` remains anywhere in .github/workflows. synthetic-ssr-monitor.yml (2 pins) has zero Supabase references and was not broken, but was the only other holdout and would have walked into the same wall on any future Supabase-touching change. SEPARATE AND STILL OPEN on #187: its Supabase secret precheck fails at exit 2 because Dependabot PRs do not receive repo secrets. That is infrastructure, independent of this fix, and #187 must be re-judged after this lands rather than assumed green. Authoring pin: claude-opus-5 / high; this session runs claude-opus-5[1m] / high -- effort MATCHES, [1m] declared."
}
