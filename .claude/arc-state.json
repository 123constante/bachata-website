{
  "version": 1,
  "arc": "supabase-defer",
  "phase": 5,
  "set_at": "2026-08-07T05:40:00Z",
  "required_model": "claude-fable-5",
  "required_effort": "high",
  "scope": [
    ".claude/arc-state.json",
    ".claude/settings.local.json",
    "app/routes/catchall.tsx",
    "perf-budgets.json",
    "scripts/check-bundle-budget.mjs",
    "src/components/auth/AuthGuard.tsx",
    "src/hooks/useAuth.tsx",
    "src/integrations/supabase/eagerAuthClient.ts",
    "src/lib/authResolution.ts",
    "src/pages/AuthCallback.tsx",
    "tests/authResolution.test.ts"
  ],
  "note": "PHASE 5, BUILT AND VERIFIED, NOT MERGED. P5's stated premise was REFUTED BY MEASUREMENT before any code was written -- the FOURTH instrument failure in this arc, same shape as the three below. The plan said P4c's merge opened an OAuth exposure window on /auth/callback and P5 must close it by warming the client on /auth* or handing the fragment to exchangeCodeForSession. Neither was needed: the callback path NEVER LOST eager construction. The auth pages are served by app/routes/catchall.tsx, NOT a framework route, and client.ts sat in the catchall's first-load static graph the whole time via TWO accidental holders -- src/components/auth/AuthGuard.tsx:5 (bundled into the AnimatedRoutes chunk) and src/components/ListingRequestForm.tsx:2 through the @/lib/supabase re-export (bundled into ComingSoonGate). Measured three ways at ebc67e8: client.ts in catchall's first-load graph = true (home = false); the served HTML for /auth/callback modulepreloads client-*.js AND vendor-supabase.js before hydration. WHAT PRODUCED THE WRONG CLAIM, and what nearly did again: the arc plan reasoned about which FILE imports the client, when the question was which ROUTE's graph contains it -- and /auth lives on a route nobody had looked at. TWO WEAKER PROOFS WERE DISCARDED en route and must not be reused: a route node's manifest `imports` array is FLATTENED for entry nodes (direct == transitive for catchall, home and AuthCallback alike), and the emitted entry chunk HOISTS its whole closure's imports, so seeing `import \"./client-*.js\"` in the built catchall chunk proves nothing about who holds the edge. The only decisive test was severing both accidental holders and rebuilding, which left catchall as the sole importer with the edge intact. WHAT P5 SHIPS: (a) src/integrations/supabase/eagerAuthClient.ts, a deliberate static import from catchall.tsx, so the edge is INTENTIONAL rather than a side effect of two unconverted files; (b) perf-budgets.json `requiredFirstLoad` + findMissingRequiredEdges/assertRequiredDeclared in check-bundle-budget.mjs -- the INVERSE rule, blocking, which fails if client.ts leaves the auth path's graph; (c) src/lib/authResolution.ts, the auth resolution state machine extracted out of AuthProvider's effect so its risky paths are testable. Auth route budgeted at 310 KB against 277.4 measured. GUARD RED IS DEMONSTRATED, NOT ASSERTED: simulating P6's end state, the guard exits 1 while the route sits at 233.6 KB, comfortably UNDER its 310 budget -- the regression reads as a 43.8 KB WIN, which is exactly why the budget alone could never catch it. PROVEN WITH REAL SIGNED-IN ROUND TRIPS (magic link via admin/generate_link for dev-1778694162986-qycote@example.test, driven in a browser against a local prod build). A, the production shape landing on /auth/callback: session persisted, fragment consumed, NO callbackError=expired. B, the harsher bare-origin case landing on / (a route with ZERO supabase edges): also passed. Two invalid runs were discarded rather than reported -- a `vite preview` without --outDir build/client served text/html for every asset so nothing hydrated, and a stale preview server served HTML referencing chunks no longer on disk. ALWAYS restart the preview server after a rebuild and confirm an asset's content_type is text/javascript before believing a browser result. NOTE for anyone reproducing: admin/generate_link stamps the Site URL over `redirect_to` regardless of the allowlist; the allowlist is enforced at /auth/v1/verify, so pass redirect_to THERE. P6 INPUTS -- CARRY ALL OF THESE: (1) signOut's SWALLOWED REJECTION, src/hooks/useAuth.tsx. The catch reports to Sentry and returns normally, so a failed sign-out is indistinguishable from a successful one: the user is told nothing, the UI proceeds as signed-out, and the session is still live -- worst on a shared or public device. /security-review flagged it as REAL but PRE-EXISTING (it is unchanged context carried through P4c/P5), which is why it was not fixed under P5's scope. It needs a real fix in P6: surface the failure to the user, or clear local session state so the UI cannot claim a sign-out that did not happen. Do NOT let it be closed as 'already reported'. (2) AuthGuard.tsx and ListingRequestForm.tsx are now FREE TO CONVERT to getSupabase() -- eagerAuthClient holds the auth edge and requiredFirstLoad guards it. This was the whole point of P5. (3) The retry card in AuthGuard is NARROWER than it looks: every AuthGuard-gated page is on the catchall, where the client is now a static import, so a client-CHUNK failure kills the route module and AuthGuard never mounts. The reachable path is resolution TIMING OUT (verified live: expired token + hanging refresh -> card, no redirect). If P6 moves gated pages to framework routes, the chunk-failure path becomes reachable and should be re-verified. (4) AUTH_RESOLVE_TIMEOUT_MS (8000) must stay BELOW AuthCallback.tsx's hardcoded 9000 fallback; both clocks start on mount. Cross-referenced in comments only -- nothing enforces it. (5) P6 still owes: lower home's budget to the measured number, promote the puller count to a ratchet (requiredFirstLoad is the both-directions counterpart, already installed), and the ship-gate.mjs gap where perf-budgets.json is not covered by the guard that covers the workflow enforcing it. (6) P6 pin per the model/effort table is claude-opus-5 / high with /code-review ultra -- required_model below is still claude-fable-5 for P5; FLIP IT BEFORE P6. (7) TWO PRODUCTION ISSUES found incidentally, written up separately and NOT folded into this arc: ~/.claude/plans/prod-issue-supabase-redirect-normalisation.md (a non-allowlisted redirect_to is silently replaced with the Site URL rather than rejected, which would drop a real user on the site root instead of /auth/callback and lose returnTo/mode handling) and ~/.claude/plans/prod-issue-dancer-profile-autocreate.md (`authenticated` has no INSERT/UPDATE on dancer_profiles, so new-user profile auto-creation fails; existing users unaffected). (8) http://localhost:8081/** was added to the Supabase redirect allowlist for P5's round trip and must be REMOVED; treat it as removed by default. --- PRIOR PHASE 4 RECORD, RETAINED VERBATIM --- PHASE 4, SPLIT INTO THREE PRs after measurement refuted the plan's premise. P4's useAuth conversion was applied and measured on 7e34cb5: home went 245.0 -> 244.9 KB gz, 3 -> 3 pullers, vendor-supabase still IN first load. The plan required 0 pullers and about -43 KB, so nothing shipped. CORRECTION TO THE PREVIOUS ENTRY (this is the point of this record): it declared 'KNOWN REMAINING EDGES (out of scope, recorded for P6): profileViewEmit.ts (event-page-only), searchClickTelemetry.ts (search-only), resolvePublicEventRef.ts (detail-loader-only)'. Two of those three are WRONG. searchClickTelemetry.ts is on home's first-load path via root.tsx:20 -> SearchProvider -> SearchOverlay:16, and resolvePublicEventRef.ts via home.tsx -> detailLoader.ts:5. Only profileViewEmit.ts was right. WHAT PRODUCED THE WRONG CLAIM: `grep -rl 'integrations/supabase/client' src/ app/` lists modules containing a literal string. It reports no route information whatsoever, so 'search-only' and 'detail-loader-only' were inferences about REACHABILITY drawn from a tool that measures MEMBERSHIP -- the module was found, its route was assumed. grep also cannot follow the three shapes every miss actually had: a barrel re-export (home.tsx -> @/lib/seo -> useEntitySlugOrId), a provider three components deep (root -> SearchProvider -> SearchOverlay -> searchClickTelemetry), and a loader helper (home -> detailLoader -> resolvePublicEventRef). Same method, three misses: the original 3-edge plan, the detailLoader edge before #207, and these two. MEASURED EDGE LIST, from the new module-level report at 7e34cb5 with useAuth unconverted: home 8 edge modules across 88 parsed (useAuth.tsx, usePopularSearches.ts, festivalsList.ts, searchClickTelemetry.ts, searchRpc.ts, searchTelemetry.ts, seo/resolvePublicEventRef.ts, seo/useEntitySlugOrId.ts); event 24 across 151. PR ORDER: 4a = this PR, extend check-bundle-budget.mjs to report module-level edges + fix this record (GUARD-CI, claude-opus-5 / high, /code-review high). 4b = cut the six non-auth edges: lazy the SearchOverlay import (cuts 4 at once), the @/lib/seo barrel re-export, detailLoader's transitive resolvePublicEventRef, festivalsList (P2 tier, claude-opus-5 / high, /code-review high; two proofs required -- 4a's module list showing only useAuth.tsx left, and SSR from rendered HTML on BOTH / and /city/london-gb, since the search cluster sits on root's path). 4c = useAuth alone, the genuine last edge, 3 -> 0 pullers and about -43 KB (claude-fable-5 / high, /code-review xhigh + /security-review). FLIP required_model TO claude-fable-5 BEFORE 4c. The 4c working diff is parked at C:/tmp/p4-useauth-REVISED.patch (the plan's patch plus three fixes: the auth subscription leaked when the provider unmounted before getSupabase() resolved, a getSupabase() rejection was unhandled and left isLoading true forever which parks AuthGuard on its spinner site-wide, and the captureException import). SearchOverlay renders behind flags.searchV5 but its IMPORT is static, so the flag does not affect the graph. The P4/P5 OAuth exposure window Ricky accepted on 2026-08-05 opens at 4c's merge, not before -- P5 should follow it closely. 4b OUTCOME -- SECOND INSTRUMENT CORRECTION, SAME SHAPE AS THE GREP ONE: the module report's COUNT was right (8) but its display made the inference wrong. It prints ONE shortest path per edge module, so 'lazy the SearchOverlay import cuts 4 at once' was read off searchRpc.ts and searchTelemetry.ts showing a root -> SearchProvider -> SearchOverlay path. Both had a SECOND, independent parent the overlay path masked -- root -> AppChrome -> GlobalHeader -> HeaderSearch -> usePublicSearch, the always-rendered omnibox, which is not flag-gated and cannot be lazied the same way. MEASURED, replacing the plan's arithmetic: home was 8 edges; cutting the overlay import took it to 3; routing the two survivors through getSupabase() took it to 1 (useAuth alone). So 8 -> 3 -> 1, over six files rather than four. The plan's own '8 -> 2' target was never derivable: it came from '4b = cut the SIX non-auth edges', but the measured 8-module list it cites contains SEVEN non-auth modules (usePopularSearches, festivalsList, searchClickTelemetry, searchRpc, searchTelemetry, seo/resolvePublicEventRef, seo/useEntitySlugOrId) -- and the acceptance proof stated one line later, 'only useAuth.tsx left', is 8 -> 1. The undercount and the target contradicted each other in the same note and neither was checked against the list sitting above them. THE RULE: a single shortest path tells you an edge EXISTS, not what cutting one parent ACHIEVES -- yield-per-cut needs the full parent set, and a masked second parent is invisible by construction. P6 must NOT infer per-cut yields from this display; either enumerate all parents per edge or re-measure after each cut. Identical failure mode to the grep audit above: a tool answering a narrower question (does an edge exist / does a file contain a string) than the one being asked of it (what does cutting this parent buy)."
}
