{
  "version": 1,
  "arc": "dancer-profile-ownership-key",
  "phase": 1,
  "set_at": "2026-08-09T15:20:00Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "scope": [
    "src/pages/AuthCallback.tsx",
    "src/components/auth/AuthGuard.tsx",
    "src/components/auth/ProfileEntryFlow.tsx",
    "src/pages/Onboarding.tsx",
    "src/pages/EditProfile.tsx",
    "src/pages/CreateProfile.tsx",
    "src/pages/CreateOrganiserProfile.tsx",
    "src/pages/PracticePartners.tsx",
    "src/pages/DancerProfile.tsx",
    "src/pages/Profile.tsx",
    "src/components/profile/DancerDashboard.tsx",
    "src/components/profile/VendorDashboard.tsx",
    "src/components/profile/ManageProfilesHub.tsx",
    "src/components/profile/ProfileEntryRouter.tsx",
    "src/modules/profile/dancerPublicProfile.ts",
    "app/routes/dancers.tsx",
    "src/hooks/useUserIds.tsx",
    "src/lib/onboardingStatus.ts",
    "src/lib/ensureDancerProfile.ts",
    "src/lib/saveMyDancerProfile.ts",
    "tests/saveMyDancerProfile.test.ts",
    "scripts/pre-ship.mjs",
    "tests/preShipEslintRatchet.test.ts",
    "tests/e2e/vendor-city-normalization-smoke.spec.ts",
    "tests/e2e/dancer-dashboard-concept-b-smoke.spec.ts"
  ],
  "note": "PHASE 1 -- close the signup funnel. LIVE USER-FACING: authenticated holds no INSERT/UPDATE on dancer_profiles, so no new signup can complete onboarding. Pin: claude-opus-5 / high, /code-review xhigh with an EXPLICIT branch target. NOT A MIGRATION -- Ricky decided 2026-08-08 to rely on trg_handle_new_dancer_profile for creation, and granting INSERT is refuted (id has no default; a BEFORE trigger mints public.person from it, moving 42501 to 23502). The fix is a client reroute onto save_my_dancer_profile_v1(p_payload jsonb), which had ZERO callers. DECISIONS (2026-08-09, do not reopen): (1) all 12 created_by reads are OWNERSHIP, zero are authorship -- repoint to id = user.id ONLY, never claimed_by (measured: the 5 claimed-linked users are a SUBSET of the 7 id-linked, so widening adds nobody and would diverge from resolve_my_person_id_v1, which ignores claimed_by -- a read that resolves where the write path RAISEs). (2) DancerProfile.tsx:85 isSelfView is a 13th ownership site, never previously enumerated -- repoint. (3) useUserIds.dancerId keeps meaning 'a persona row exists'; consumers gain an explicit completeness check. (4) ensureDancerProfile.ts is RETIRED, not repaired -- this CLOSES arc-shape step 3, so the migration retargeting ensure_dancer_profile at dancer_profiles must NOT be written. (5) EditProfile's loader is fixed to the real columns; the hard city gate STAYS. (6) nameless public stubs are OUT OF SCOPE. MEASURED CONTRACT FACTS not in the plan file, all read off pg_get_functiondef and live counts: (a) save_my_dancer_profile_v1 HAS NO meta_data ARM, so the client can no longer stamp meta_data.onboarding_status. Survivable ONLY because inferOnboardingStatusFromDancer falls back to first_name && based_city_id, and ZERO of the 7 real rows carry an explicit status (5 of 7 already infer completed). If any future path writes an explicit in_progress it LATCHES UNCLEARABLE and re-closes the funnel -- the gate would bounce forever while every save succeeds. (b) The accepted key set is first_name, surname, avatar_url, based_city_id, nationality, dance_role, instagram, facebook, whatsapp, website_url at top level, plus a dancer_details sidecar object. NOT city_id, NOT website, NOT photo_url -- website/photo_url are MIRRORED from website_url/avatar_url on every save. (c) dancer_profiles has NO city_id, city, years_dancing, partner_role or dancing_start_date column, yet EditProfile:193 writes city_id and :195 writes dancing_start_date, and CreateProfile:806 writes dancing_start_date -- all would 42703 even with grants. The form's dancing_start_date must map onto the sidecar's dance_started_year (INTEGER year, not a date string). (d) the mirror is unconditional and survivor-authoritative: a save against a SIDECAR-LESS row seeds the sidecar empty and then WIPES the legacy arrays. Safe today at 0 sidecar-less rows; re-measure before assuming. (e) rixz@ymail.com's 10 authored rows do NOT include their own profile, so today these reads resolve, for every user alive, to either nothing or ten strangers -- and AuthCallback:125 lacks .maybeSingle(), takes [0] of an unordered set and UPDATEs it, a live write into another person's row. ACCEPTED DEBT, do not fix: the RPC cannot clear a field (every column is COALESCE(NULLIF(x,''), dp.x)), so EditProfile loses the ability to blank instagram/whatsapp/website_url/nationality. The save still SUCCEEDS, so no test catches it. DO NOT SHIP A SUBSET: the 3 sites an earlier session converted (local-only commit 50c7d36) are worse than nothing alone -- AuthCallback finds the persona and routes to /profile, then AuthGuard bounces straight back to /onboarding."
}
