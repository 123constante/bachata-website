{
  "version": 1,
  "arc": "edge-config-governance",
  "phase": 2,
  "set_at": "2026-08-22T00:00:00Z",
  "closed_at": null,
  "closed_note": null,
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "actual_model": "claude-sonnet-5",
  "deviation": "Session started on claude-sonnet-5 (Auto Mode session default), not the plan's required claude-opus-5. Declared and recorded per CLAUDE.md (a mismatch is declared and recorded, never a halt) -- proceeding without a stop, effort held at high for this phase's guard-authoring work.",
  "scope": [
    ".github/workflows/prod-smoke.yml",
    ".github/workflows/seo-check.yml",
    ".github/workflows/og-image-check.yml",
    ".github/workflows/synthetic-ssr-monitor.yml",
    ".github/workflows/gsc-health-check.yml",
    ".github/workflows/sourcemap-check.yml",
    ".github/workflows/weekly-digest.yml",
    ".github/workflows/workflow-lint.yml",
    "scripts/lint-workflow-notification.mjs",
    "scripts/prove-entry-point-dispatch.mjs"
  ],
  "note": "P2 of the edge-config-governance arc (plan: ~/.claude/plans/i-pasted-this-link-gentle-cray.md). Part A: copy db-contract-check.yml's failure-notification + close-on-green pattern into the four probe workflows that went red silently for ~14h on 2026-08-21/22 (prod-smoke, seo-check, og-image-check prod arm only, synthetic-ssr-monitor). Part B: a new lint-workflow-notification.mjs (wired into workflow-lint.yml) that refuses a schedule-triggered job referencing production with no notification step -- built on a real YAML parse, reusing check-workflow-artifact-policy.mjs's review-hardened readTriggers/conditionIsFailureGate/scheduleCanReach rather than reinventing them (an early hand-rolled line-scanner draft reintroduced three bugs those functions were already fixed against: a negated failure() condition, a commented-out uses: line, and a block-scalar VALUE shaped like 'steps:' -- caught by /code-review xhigh and fixed by the rewrite, mutation-tested). The lint's own real scan of the repo (not just the four named files) surfaced three more live instances of the SAME defect -- gsc-health-check.yml, sourcemap-check.yml (schedule-triggered prod probes with no notify step, the domain only in the invoked script's own fallback default, not in the workflow YAML) and weekly-digest.yml (same shape, a report-delivery job rather than a health probe) -- all three repaired alongside the four named in scope. prod-smoke.yml and seo-check.yml additionally widened their notify scope to include deployment_status (the post-deploy run itself, previously excluded on weak reasoning the review flagged); synthetic-ssr-monitor.yml dropped the cancelled() clause it copied from db-contract-check.yml, because this file (unlike db-contract-check.yml) has concurrency.cancel-in-progress:true, so an ordinary workflow_dispatch overlapping a schedule tick would file a false-positive issue. Single PR-sized phase; do not start P3+."
}
