{
  "version": 1,
  "arc": "supabase-defer",
  "phase": 3,
  "set_at": "2026-08-06T10:53:00Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "scope": [
    ".claude/arc-state.json",
    "app/detailLoader.ts"
  ],
  "note": "PRE-P4 CLEANUP -- 4th static edge discovered. The plan's 3-edge analysis (AuthProvider, CityContext, eventRpcs) was INCOMPLETE. A 4th edge, home.tsx -> app/detailLoader.ts -> client.ts, was missed by the original audit and only surfaced because P4's puller count refused to reach 0. The audit used grep -rl 'integrations/supabase/client' which misses edges reached through app/ loader modules — P5 and P6 must not treat that edge list as exhaustive. What caught it was P0's bundle-budget instrument, not the plan and not a review. MITIGATION: convert detailLoader to lazy accessor (same P2-tier pattern: resolveEntityInLoader and fetchBakedOgImage are both async, await getSupabase() at function start). MEASUREMENT AFTER DETAILLOADER: home 244.9 KB gz / 63 chunks / 3 pullers (unchanged — chunk-level metric only collapses when the LAST edge goes, which is P4). Removes one static edge so P4's AuthProvider change becomes the genuine final edge. EDGES STILL STATIC: only AuthProvider and CityProvider remain (both on root.tsx first-load path). P4 will cut AuthProvider. After P4: puller count should drop to measure the win from both P3 and P4 combined."
}
