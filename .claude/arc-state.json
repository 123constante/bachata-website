{
  "version": 1,
  "arc": "multicountry-map",
  "plan": "i-want-find-a-imperative-curry.md",
  "phase": "P0",
  "set_at": "2026-08-28T18:20:00Z",
  "required_model": "claude-opus-5",
  "required_effort": "high",
  "note": "P0 is the arc PRECONDITION, not an arc phase: ship the basemap half of fix/home-map-basemap-and-controls, which is what removes the CARTO 'API KEY REQUIRED' watermark from production. Model/effort taken from the tier table (BUILD-visual shipping straight to prod on merge); the plan states none for P0. NO `scope` KEY, DELIBERATELY: declaring one puts pre-ship's scopeDrift into `declared` mode, where ANY worktree file outside the list is a hard error. This worktree carries 11 untracked mockup HTMLs (kept on purpose -- home-map-festivals-tab-mockup.html is prior art for P4) and a dirty scripts/check-occurrence-delete-booking-safety.mjs belonging to another session, so declared mode cannot pass without stashing someone else's work. Inferred mode is the repo's normal posture and WARNs on the foreign guard. That claim was WRONG when written and the miscount is the lesson: a THIRD foreign file was in the worktree, src/lib/encodings.generated.ts -- untracked and NOT gitignored, an admin-repo artefact (its own header names scripts/gen-encodings.mjs and supabase/encodings/registry.json, neither of which exists here) with zero references anywhere in this tree, so a `git add -A` would have committed a stray generated file into the public repo's source. Removed 2026-09-01. Enumerate the untracked set before asserting what inferred mode warns on. Re-add `scope` only alongside a clean worktree. This file also carries a `plan` key that the previous arc (edge-config-governance) never had, which is why check-plan-hygiene's lintArcState skipped its cross-check for that arc's whole life and let its plan pointer stay wrong."
}
